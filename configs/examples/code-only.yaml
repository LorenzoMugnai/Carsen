knowledge:
  id: code_only
  name: Code Only Example
storage:
  qdrant_url: http://qdrant:6333
  collection: kb_code_only
  data_directory: /data/ariadne/code-only
server:
  transport: http
  host: 127.0.0.1
  port: 8765
sources:
  code:
    - path: /workspace/code
      repository_name: example-code
      type: code
